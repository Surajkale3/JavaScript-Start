const func= ()=>
{ 

    // push inbuilt function
    const arr = [1,2,3,4]
    arr.push(6);
    console.log(arr);

    // pop inbuilt function
    const plants = ['broccoli', 'cauliflower', 'cabbage', 'kale', 'tomato'];
    plants.pop();
     console.log(plants);

    
}
let arry= func();
console.log(arry);